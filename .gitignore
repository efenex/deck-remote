# compiled binary
/deck-remote
# logs / scratch
*.log
*.bak.*
# internal design mockups (contain sample/private session names) — not published
mockups/
# local-only working notes (machine specifics / handoff) — never published
/TODO.md
/HANDOFF.md

# local install runbooks (machine specifics) and macOS cruft
/INSTALL-RUNBOOK-*.md
.DS_Store

# bd workspace gate file (beside .beads)
.beads.gate.lock
